---
"@liustack/pptpress": minor
---

Architecture diagrams are now a layer stack: index on the left, layer name in the middle, notes on the right, hairline rules between rows. Too many layers fail validate instead of folding into a leftover +N. The renderer no longer drops rows.
