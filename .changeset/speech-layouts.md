---
"@liustack/pptpress": minor
---

Three pin-only speech layouts (`stat-hero`, `one-evidence`, `mono-bleed`). Each declares `chrome: "none"`, so pinning one skips the brand footer, logo, and theme motif. `mono-bleed` also paints its own primary field. Auto-selection is unchanged: the new ids never enter the pool.
