---
"@liustack/pptpress": patch
---

Rail-numbered content pages on assigned themes no longer crush the heading's first character with the `{chapter}.{n}` badge. The layout still paints the badge. The heading treatment yields by the same 20px gap the native title already used.
