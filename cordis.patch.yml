# dsh bundle layer: mount the pptwise skill plugin (dsh/index.js via the
# package root export; bin stays the CLI, nothing imports the root otherwise).
- insert:
    - id: pptwise
      name: '@liustack/pptwise'
