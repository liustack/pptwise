# dependencies
node_modules/

# build output
dist/
.e2e-out/

# assembled from deck.spec.json + pages/ — regenerate with `pptpress assemble`
examples/launch-deck/deck.json

# logs
*.log

# lockfiles from other package managers
bun.lockb
bun.lock
package-lock.json

# environment variables
.env
.env.production
.dev.vars

# OS / editor
.DS_Store
.idea/
.vscode
.venv

# framework / deploy caches
.astro/
.netlify/
.wrangler

# test output
test-output
test/output
coverage/

# benchmark run outputs are regenerable, ignored wholesale
# (tests/bench/fixtures/ is a different tracked path, unaffected)
# one rule covers every run-output dir: results/ (main bank), results-probe/
# (evidence-gate probes), results-archive/<date>/ (kept snapshots of past runs)
tests/bench/results*/

# agent workspaces (local, never committed)
.agent/
.issues/
.superpowers
.claude/settings.local.json

# playwright-cli session artifacts (agent browser sessions)
.playwright-cli/
